---
phase: 04-sentiment-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - Scripts/news.py
  - verify_phase4_01.py
autonomous: true
requirements:
  - SENT-01
must_haves:
  truths:
    - "feedparser and vaderSentiment are available in the environment"
    - "SentimentAnalyzer can fetch RSS headlines for a ticker from Yahoo Finance"
    - "SentimentAnalyzer averages VADER compound scores for headlines"
    - "Sentiment score fetching has a 15-minute TTL cache per ticker"
  artifacts:
    - path: "Scripts/news.py"
      provides: "SentimentAnalyzer class and get_ticker_sentiment function"
    - path: "verify_phase4_01.py"
      provides: "Standalone verification script for sentiment fetching and scoring"
  key_links:
    - from: "Scripts/news.py"
      to: "Yahoo Finance RSS"
      via: "feedparser.parse with User-Agent"
---

<objective>
Implement the sentiment pipeline logic, including fetching Yahoo Finance RSS headlines and scoring them with VADER.

Purpose: Provide a lightweight, confirming sentiment score for ticker symbols without requiring external API keys.
Output: Updated requirements.txt, enhanced Scripts/news.py, and a new verification script.
</objective>

<execution_context>
@C:/Users/Palim/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-sentiment-pipeline/04-RESEARCH.md
@Scripts/news.py
@Scripts/utils.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update dependencies</name>
  <files>requirements.txt</files>
  <action>
    Add `feedparser==6.0.11` and `vaderSentiment==3.3.2` to `requirements.txt`.
    Run `pip install -r requirements.txt` to ensure they are available.
  </action>
  <verify>
    python -c "import feedparser; from vaderSentiment.vaderSentiment import SentimentIntensityAnalyzer; print('Success')"
  </verify>
  <done>Dependencies installed and importable.</done>
</task>

<task type="auto">
  <name>Task 2: Implement SentimentAnalyzer in Scripts/news.py</name>
  <files>Scripts/news.py</files>
  <action>
    1. Import `feedparser` and `from vaderSentiment.vaderSentiment import SentimentIntensityAnalyzer`.
    2. Define `_sentiment_cache = {}` at the module level.
    3. Implement `SentimentAnalyzer` class (or simply update existing news logic) to:
       - Fetch RSS headlines from `https://feeds.finance.yahoo.com/rss/2.0/headline?s={ticker}&region=US&lang=en-US`.
       - Use a browser-like User-Agent in `feedparser.parse` (e.g., 'Mozilla/5.0').
       - Score the last 10 headlines using `SentimentIntensityAnalyzer.polarity_scores(title)['compound']`.
       - Average the scores. Return 0.0 (neutral) if no headlines found.
    4. Implement `get_ticker_sentiment(ticker)` with a 15-minute TTL cache logic (mirroring the VIX pattern in `Scripts/utils.py`).
    5. Retain existing `fetch_articles_for_symbol` but mark as legacy or secondary if needed.
  </action>
  <verify>
    Check for SentimentAnalyzer class and get_ticker_sentiment function in Scripts/news.py.
  </verify>
  <done>Sentiment analysis logic implemented with RSS fetching and TTL caching.</done>
</task>

<task type="auto">
  <name>Task 3: Create verification script verify_phase4_01.py</name>
  <files>verify_phase4_01.py</files>
  <action>
    Create a script that:
    1. Imports `get_ticker_sentiment` from `Scripts.news`.
    2. Fetches sentiment for a known ticker (e.g., 'AAPL').
    3. Prints the score.
    4. Fetches again immediately and asserts that it's from cache (by checking if it returned the same value or by instrumenting the cache).
    5. Validates that a non-existent ticker returns 0.0 without crashing.
  </action>
  <verify>
    python verify_phase4_01.py
  </verify>
  <done>Verification script passes and demonstrates RSS fetch + scoring.</done>
</task>

</tasks>

<verification>
Run `python verify_phase4_01.py` to confirm the pipeline works independently of the trading loop.
</verification>

<success_criteria>
- `feedparser` and `vaderSentiment` are in `requirements.txt`.
- `Scripts/news.py` contains `get_ticker_sentiment(ticker)` with TTL caching.
- `verify_phase4_01.py` successfully fetches and scores AAPL sentiment.
</success_criteria>

<output>
After completion, create `.planning/phases/04-sentiment-pipeline/04-01-SUMMARY.md`
</output>
