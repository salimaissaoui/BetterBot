---
phase: 03-risk-gating
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [requirements.txt, Scripts/advanced_features.py, Scripts/main.py, verify_phase3_01.py]
autonomous: true
requirements: [RISK-01]

must_haves:
  truths:
    - "MarketRegimeDetector can save its fitted state to a file"
    - "MarketRegimeDetector can load its state from a file on initialization"
    - "The bot fits a new regime model using S&P 500 data if no persisted model exists at startup"
  artifacts:
    - path: "regime_model.joblib"
      provides: "Persisted market regime model state"
    - path: "Scripts/advanced_features.py"
      provides: "MarketRegimeDetector with persistence methods"
    - path: "verify_phase3_01.py"
      provides: "Automated verification for model persistence"
  key_links:
    - from: "Scripts/main.py"
      to: "Scripts/advanced_features.py"
      via: "initialize_bot calls load_model/fit_regime_detector"
---

<objective>
Implement persistence for the MarketRegimeDetector and ensure it is initialized at bot startup.

Purpose: To ensure the bot has a valid market regime model ready at all times, avoiding 'unknown' regimes and providing consistent risk gating.
Output: Persistent regime detector model and startup initialization logic.
</objective>

<execution_context>
@C:/Users/Palim/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@Scripts/advanced_features.py
@Scripts/main.py
@requirements.txt

<interfaces>
From Scripts/advanced_features.py:
```python
class MarketRegimeDetector:
    def __init__(self, lookback_window: int = 252): ...
    def calculate_regime_features(self, data: pd.DataFrame) -> pd.DataFrame: ...
    def fit_regime_detector(self, market_data: Dict[str, pd.DataFrame]): ...
    def predict_regime(self, data: pd.DataFrame) -> str: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update requirements and add persistence to MarketRegimeDetector</name>
  <files>requirements.txt, Scripts/advanced_features.py</files>
  <action>
    1. Add `yfinance` to `requirements.txt`.
    2. In `Scripts/advanced_features.py`:
       - Import `joblib`.
       - Add `save_model(self, path="regime_model.joblib")` to `MarketRegimeDetector`. It should save: `regime_model`, `scaler`, `pca`, `feature_columns`, and `regimes`.
       - Add `load_model(self, path="regime_model.joblib")` to `MarketRegimeDetector`. It should load the saved components and return `True` on success, `False` otherwise.
       - Update `fit_regime_detector` to call `self.save_model()` after successful fitting.
  </action>
  <verify>
    Run `python -c "from Scripts.advanced_features import MarketRegimeDetector; mrd = MarketRegimeDetector(); print('Methods exist:', hasattr(mrd, 'save_model') and hasattr(mrd, 'load_model'))"`
  </verify>
  <done>Requirements updated and MarketRegimeDetector supports persistence.</done>
</task>

<task type="auto">
  <name>Task 2: Wire regime detector initialization in main.py</name>
  <files>Scripts/main.py</files>
  <action>
    Update `initialize_bot()` in `Scripts/main.py`:
    1. Import `MarketRegimeDetector` from `.advanced_features`.
    2. Try to load an existing model using `load_model()`.
    3. If loading fails:
       - Use `yfinance` to fetch `^GSPC` (S&P 500) historical data (last 5 years).
       - Prepare it as a dictionary `{ '^GSPC': df }`.
       - Call `fit_regime_detector()` with this data.
    4. Ensure the `MarketRegimeDetector` instance is accessible for trading (e.g., via the global `advanced_model` or similar).
  </action>
  <verify>
    `python Scripts/main.py` starts up and logs either model loading or fitting of the regime detector.
  </verify>
  <done>Regime detector is initialized at bot startup.</done>
</task>

<task type="auto">
  <name>Task 3: Create verification script for regime persistence</name>
  <files>verify_phase3_01.py</files>
  <action>
    Create a script `verify_phase3_01.py` that:
    1. Creates a `MarketRegimeDetector`.
    2. Mocks/Generates dummy market data.
    3. Fits the detector.
    4. Verifies `regime_model.joblib` is created.
    5. Creates a second detector instance and calls `load_model()`.
    6. Verifies the loaded model can `predict_regime()` on same data.
  </action>
  <verify>
    `python verify_phase3_01.py` passes.
  </verify>
  <done>Model persistence is verified.</done>
</task>

</tasks>

<verification>
Check that `regime_model.joblib` exists after the first successful fit/startup.
Check logs to confirm initialization path.
</verification>

<success_criteria>
- `yfinance` is installed.
- `MarketRegimeDetector` has save/load methods.
- `regime_model.joblib` is created on first run.
- Bot initialization includes regime detector setup.
</success_criteria>

<output>
After completion, create `.planning/phases/03-risk-gating/03-01-SUMMARY.md`
</output>
