---
phase: 03-risk-gating
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [Scripts/utils.py, Scripts/trade.py, verify_phase3_02.py]
autonomous: true
requirements: [RISK-02]

must_haves:
  truths:
    - "VIX is fetched from yfinance with a 15-minute TTL cache"
    - "Position size is reduced by 50% when VIX > 30"
    - "Position size is reduced by 50% for Neutral regime, and by 75% for Bearish/Volatile regimes"
    - "The applied multipliers and VIX level are logged in the TradeLog entry_reason"
  artifacts:
    - path: "Scripts/utils.py"
      provides: "get_current_vix() function with caching"
    - path: "Scripts/trade.py"
      provides: "submit_ml_sized_order with multiplier logic"
    - path: "verify_phase3_02.py"
      provides: "Verification of position sizing multiplier logic"
  key_links:
    - from: "Scripts/trade.py"
      to: "Scripts/utils.py"
      via: "calls get_current_vix()"
---

<objective>
Implement VIX-based and Regime-based position sizing multipliers.

Purpose: To protect capital by reducing exposure during hostile market conditions (high VIX or bearish/volatile regimes).
Output: VIX fetching utility and updated position sizing logic with logging.
</objective>

<execution_context>
@C:/Users/Palim/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@Scripts/utils.py
@Scripts/trade.py
@Scripts/database.py

<interfaces>
From Scripts/trade.py:
```python
def submit_ml_sized_order(symbol, side, prediction_confidence, current_price=None): ...
```

From Scripts/database.py (TradeLog):
```python
class TradeLog(Base):
    ...
    entry_reason        = Column(String(200), nullable=True)
    regime_at_decision  = Column(String(20), nullable=True)
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add get_current_vix to utils.py</name>
  <files>Scripts/utils.py</files>
  <action>
    1. Import `yfinance` as `yf`.
    2. Add `get_current_vix()` to `Scripts/utils.py`.
    3. Implement a 15-minute TTL cache for the VIX value using a module-level variable and timestamp.
    4. Fetch `^VIX` using `yf.Ticker("^VIX").history(period="1d")`. Return the latest `Close` price.
  </action>
  <verify>
    Run `python -c "from Scripts.utils import get_current_vix; print('VIX:', get_current_vix())"`
  </verify>
  <done>VIX fetching with caching is implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Apply multipliers in submit_ml_sized_order</name>
  <files>Scripts/trade.py</files>
  <action>
    Update `submit_ml_sized_order` in `Scripts/trade.py`:
    1. Import `get_current_vix` from `.utils`.
    2. Add `market_regime` as an optional argument to `submit_ml_sized_order`.
    3. Inside `submit_ml_sized_order`:
       - Call `get_current_vix()`.
       - Determine `vix_multiplier`: `1.0` if VIX <= 30, `0.5` if VIX > 30.
       - Determine `regime_multiplier`: `1.0` if Bullish, `0.5` if Neutral, `0.25` if Bearish or Volatile.
       - Calculate `final_multiplier = vix_multiplier * regime_multiplier`.
       - Multiply the `qty` (returned from `ml_position_sizer`) by `final_multiplier`.
       - Ensure `qty` remains an integer and is at least 1 if the original `qty` was >= 1.
       - Log the multipliers, VIX level, and regime in `TradeLog.entry_reason` (append to existing or format as structured text).
    4. Update calls to `submit_ml_sized_order` in `execute_trade` and `hourly_portfolio_scan` to pass the detected `market_regime`.
  </action>
  <verify>
    Verify that `TradeLog` entries contain the multiplier and VIX info in the `entry_reason`.
  </verify>
  <done>Position sizing multipliers are applied and logged.</done>
</task>

<task type="auto">
  <name>Task 3: Create verification script for position sizing multipliers</name>
  <files>verify_phase3_02.py</files>
  <action>
    Create a script `verify_phase3_02.py` that:
    1. Mocks `get_current_vix()` to return different levels (e.g., 20, 35).
    2. Mocks `ml_position_sizer.calculate_position_size()` to return a fixed quantity (e.g., 100).
    3. Tests `submit_ml_sized_order` with various combinations of VIX and Regime.
    4. Asserts the final quantity matches expected values (e.g., VIX 35 + Bearish = 100 * 0.5 * 0.25 = 12.5 -> 12).
  </action>
  <verify>
    `python verify_phase3_02.py` passes.
  </verify>
  <done>Position sizing multiplier logic is verified.</done>
</task>

</tasks>

<verification>
Check that `TradeLog` entries for new trades show the correct regime, VIX, and multiplier.
Verify that large VIX spikes result in smaller position sizes in paper trading.
</verification>

<success_criteria>
- VIX is successfully fetched and cached.
- Multipliers are correctly applied based on VIX and Regime.
- All multipliers and VIX data are logged for every entry decision.
</success_criteria>

<output>
After completion, create `.planning/phases/03-risk-gating/03-02-SUMMARY.md`
</output>
