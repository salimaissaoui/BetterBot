---
phase: between-milestones
task: 1
total_tasks: 3
status: in_progress
last_updated: 2026-02-28T04:02:43.374Z
---

<current_state>
Mid-way through /gsd:new-milestone for v1.1. Context exhausted before questioning began.
Loaded: PROJECT.md, MILESTONES.md (v1.0 complete). No MILESTONE-CONTEXT.md exists.
Next step is to ask the user what they want to build in v1.1.
</current_state>

<completed_work>

- v1.0 milestone archived: ROADMAP.md collapsed, REQUIREMENTS.md deleted, phases moved to milestones/v1.0-phases/, git tagged v1.0, pushed to remote
- yfinance upgraded 0.2.51 → 1.2.0 (fixes ^VIX JSONDecodeError)
- All 5 verify scripts fixed (load_dotenv + active_positions contamination in verify_vix_log)
- Diagnosed sell/short bias in live bot (see context below)
- All test fixes committed and pushed

</completed_work>

<remaining_work>

- Task 1: Complete /gsd:new-milestone questioning — ask user what they want in v1.1
- Task 2: Define requirements (REQUIREMENTS.md)
- Task 3: Create roadmap (ROADMAP.md, phases 6+)

</remaining_work>

<decisions_made>

- v1.0 archived with Known Gap: VAL-01 (14-day paper trading session not yet completed)
- User is currently running the bot in paper trading mode
- Sell/short bias diagnosed but NOT yet fixed — user was in the middle of /gsd:new-milestone when context ran out

</decisions_made>

<blockers>

- SELL/SHORT BIAS (active, unfixed): The bot has a strong tendency to sell/short on almost every symbol.
  ROOT CAUSE 1: TARGET_THRESHOLD=0.003 (0.3%) is too strict — most bars don't move +0.3%, so class 0 (bearish) dominates training data and the model defaults to low probability predictions.
  ROOT CAUSE 2: want_to_short=(prob < 0.49) has no position guard, fires on every symbol every bar when model is bearish-biased.
  PROPOSED FIXES (not yet implemented):
    1. Make SHORT_THRESHOLD configurable via .env (currently hardcoded at 0.49 in execute_trade) — raise to ~0.35
    2. Or disable shorting entirely with a flag
    3. Lower TARGET_THRESHOLD from 0.003 → 0.001 to rebalance training labels

</blockers>

<context>
The user ran /gsd:new-milestone to start v1.1 planning. We got through loading context
but hit 100% context usage before the questioning step.

Key open question for v1.1: Should the milestone focus on:
  A) Fixing the sell/short bias + completing VAL-01 validation
  B) New features (from v2 backlog: EXIT-06 partial profits, EXIT-07 session-close flat,
     RISK-03 HMM regime states, RISK-04 max drawdown halt, INFRA-01 ib_insync→ib_async)
  C) Both — fix bias first (1-2 phases) then add features

The user was actively running the bot when they asked about the bias, suggesting
they may want a quick fix before planning a full milestone.
</context>

<next_action>
Start with: Ask user whether they want to fix the sell/short bias as part of v1.1,
or fix it now as a quick hotfix before the milestone planning begins.
Then proceed with /gsd:new-milestone questioning.

Resume with: /gsd:resume-work OR just /clear and start fresh with /gsd:new-milestone
</next_action>
