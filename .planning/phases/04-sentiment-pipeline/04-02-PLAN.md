---
phase: 04-sentiment-pipeline
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified: [Scripts/trade.py, Scripts/main.py, verify_phase4_02.py]
autonomous: true
requirements: [SENT-02]

must_haves:
  truths:
    - "New entry signals are suppressed if the ticker sentiment score is < -0.05"
    - "Sentiment score is recorded in TradeLog for every entry"
    - "Suppression events are logged with the sentiment score and ticker"
    - "Hourly scan for new opportunities also respects the sentiment gate"
  artifacts:
    - path: "Scripts/trade.py"
      provides: "Sentiment gate in execute_trade and score logging in _log_entry_to_db"
    - path: "Scripts/main.py"
      provides: "Sentiment gate in hourly_portfolio_scan for new opportunities"
    - path: "verify_phase4_02.py"
      provides: "Integration test for the sentiment gate with mocked scores"
  key_links:
    - from: "Scripts/trade.py"
      to: "Scripts/news.py"
      via: "get_ticker_sentiment(symbol) call"
---

<objective>
Integrate the sentiment gate into the trading loop and hourly portfolio scan, ensuring that negative sentiment suppresses entries and is logged for observability.

Purpose: Prevent entries when ticker-specific news is significantly negative.
Output: Wired entry gates in Scripts/trade.py and Scripts/main.py, plus an integration test.
</objective>

<execution_context>
@C:/Users/Palim/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-sentiment-pipeline/04-RESEARCH.md
@.planning/phases/04-sentiment-pipeline/04-01-SUMMARY.md
@Scripts/trade.py
@Scripts/main.py
@Scripts/news.py
</context>

<interfaces>
From Scripts/news.py:
```python
def get_ticker_sentiment(ticker: str) -> float:
    """Returns aggregated VADER score for ticker headlines. -1.0 to 1.0."""
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wire sentiment gate into Scripts/trade.py</name>
  <files>Scripts/trade.py</files>
  <action>
    1. Import `get_ticker_sentiment` from `.news`.
    2. In `execute_trade`, before the `want_to_buy` / `want_to_short` checks (but after the circuit breaker), fetch the sentiment score.
    3. If `sentiment_score < -0.05`, log a message: `[symbol] Entry suppressed by sentiment: {score:.3f}` and `continue` to the next ticker.
    4. Store the `sentiment_score` in a local variable to be used for logging.
    5. Update `_log_entry_to_db` to accept `sentiment_score` and save it to the `TradeLog` model.
    6. Ensure the `sentiment_score` is passed to `_log_entry_to_db` during successful entries.
  </action>
  <verify>
    Check for get_ticker_sentiment import and gate logic in execute_trade.
  </verify>
  <done>Entry signals are filtered by sentiment and scores are logged to DB.</done>
</task>

<task type="auto">
  <name>Task 2: Wire sentiment gate into Scripts/main.py</name>
  <files>Scripts/main.py</files>
  <action>
    1. Import `get_ticker_sentiment` from `.news`.
    2. In `hourly_portfolio_scan`, inside the loop for `new_opportunities`:
       - Fetch `sentiment_score = get_ticker_sentiment(symbol)`.
       - If `sentiment_score < -0.05`, skip adding the opportunity.
       - Log the suppression if it happens.
    3. (Optional but recommended) Do the same for `BUY_MORE` actions in `hourly_portfolio_scan`.
  </action>
  <verify>
    Check for sentiment checks in hourly_portfolio_scan.
  </verify>
  <done>Hourly scan opportunities are also gated by sentiment.</done>
</task>

<task type="auto">
  <name>Task 3: Create verification script verify_phase4_02.py</name>
  <files>verify_phase4_02.py</files>
  <action>
    Create a script that:
    1. Mocks `Scripts.news.get_ticker_sentiment` to return -1.0.
    2. Calls `execute_trade` with a strong buy signal for a ticker.
    3. Asserts (via logs or database) that no order was placed.
    4. Mocks `get_ticker_sentiment` to return 0.5.
    5. Calls `execute_trade` again and asserts that an order was placed and `sentiment_score` 0.5 was logged in `TradeLog`.
  </action>
  <verify>
    python verify_phase4_02.py
  </verify>
  <done>Sentiment gate integration verified with mocked data.</done>
</task>

</tasks>

<verification>
Run `python verify_phase4_02.py` to confirm the integration works as expected.
</verification>

<success_criteria>
- Entries are suppressed when mocked sentiment is -1.0.
- Entries proceed when mocked sentiment is 0.5.
- `TradeLog` shows correct `sentiment_score` for successful entries.
- `hourly_portfolio_scan` skip negative sentiment opportunities.
</success_criteria>

<output>
After completion, create `.planning/phases/04-sentiment-pipeline/04-02-SUMMARY.md`
</output>
